import { AppShell } from "@/components/layout/app-shell";
import { SimuladorView } from "./simulador-view";

export default function SimuladorPage() {
  return (
    <AppShell>
      <SimuladorView />
    </AppShell>
  );
}