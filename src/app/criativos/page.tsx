import { AppShell } from "@/components/layout/app-shell";
import { CriativosView } from "./criativos-view";

export default function CriativosPage() {
  return (
    <AppShell>
      <CriativosView />
    </AppShell>
  );
}